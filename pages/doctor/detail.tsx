import withLayoutBasic from "@/libs/components/layout/LayoutBasic";
import { Container } from "@mui/material";

const DoctorDetail = () => {
  return <Container>Doctor Detail</Container>;
};

export default withLayoutBasic(DoctorDetail);
